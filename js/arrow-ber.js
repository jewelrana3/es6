const add = (first,second) => first + second;
const getElement = (first,last) => first + ' ' + last;
const number = (a,b) => a*b;
const numberSide = add(7*8)
console.log(numberSide)
console.log('kfkf')
